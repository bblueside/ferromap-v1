{
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "frontend": "npm run dev --workspace frontend",
    "backend": "npm start --workspace backend",
    "fullstack": "npm run frontend && npm run backend"
  }
}
