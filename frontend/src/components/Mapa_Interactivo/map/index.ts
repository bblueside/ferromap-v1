/**
 * Capa de mapa: el `MapContainer` de Leaflet y las capas que se dibujan de
 * forma imperativa sobre él (marcadores de calor, GeoJSON de plantas/rutas,
 * control de zoom). Nada aquí se renderiza fuera del `<MapContainer>`; las
 * capas son detalle interno de `InteractiveMap`.
 */
export { InteractiveMap } from "./InteractiveMap";
