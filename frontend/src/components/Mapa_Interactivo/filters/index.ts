/**
 * Sidebar de filtros. Los paneles que despliega (`FilterPanel` como primitivo
 * de layout; `CheckboxFilterPanel` y `ToggleFilterPanel` como variantes de
 * contenido) son detalle interno de `Sidebar`. Todo el estado vive en
 * `@/lib/useFilterState`.
 */
export { Sidebar } from "./Sidebar";
