/**
 * API pública del feature "Mapa Interactivo".
 *
 * El resto de la app sólo debe importar desde aquí. La estructura interna
 * (`map/`, `filters/`, `panels/`, `popups/`) es un detalle de implementación y
 * puede reorganizarse sin tocar a los consumidores.
 */
export { MapDashboard } from "./MapDashboard";
